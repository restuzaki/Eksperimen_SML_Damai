name: CI Pipeline & Docker Build

on:
  push:
    branches:
      - main
    paths:
      - "Workflow-CI/**"
      - ".github/workflows/ci_training.yml"

jobs:
  mlops-pipeline:
    runs-on: ubuntu-latest

    env:
      MLFLOW_TRACKING_USERNAME: restuzaki
      MLFLOW_TRACKING_PASSWORD: ${{ secrets.DAGSHUB_TOKEN }}
      MLFLOW_TRACKING_URI: https://dagshub.com/restuzaki/Eksperimen_SML_Damai.mlflow

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install mlflow dagshub pandas scikit-learn

      - name: Run MLflow Project (Training Model)
        run: |
          echo "Menjalankan pelatihan model otomatis..."
          mlflow run Workflow-CI/MLProject/ --env-manager=local --experiment-name="Phishing_Detection_CI_Pipeline"

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Docker Image
        run: |
          cat << 'EOF' > build_script.py
          import mlflow
          import os
          import time
          from mlflow.tracking import MlflowClient

          mlflow.set_tracking_uri(os.environ.get("MLFLOW_TRACKING_URI"))
          client = MlflowClient()

          print("Menunggu 20 detik agar DagsHub sinkron...")
          time.sleep(20)

          print("Mencari Run ID terbaru...")
          df = mlflow.search_runs(experiment_names=['Phishing_Detection_CI_Pipeline'], filter_string="status = 'FINISHED'")
          target_run_id = df.sort_values('start_time', ascending=False).iloc[0].run_id
          print(f"Target Run ID: {target_run_id}")

          print("Mendownload seluruh artefak (root directory)...")
          success = False
          for attempt in range(4):
              try:
                  # Menggunakan path="" untuk mengunduh semua file tanpa mempedulikan nama foldernya
                  client.download_artifacts(run_id=target_run_id, path="", dst_path='./local_model_dir')
                  print("Download artefak berhasil.")
                  success = True
                  break
              except Exception as e:
                  print(f"Percobaan {attempt+1} gagal: {e}")
                  time.sleep(15)

          if not success:
              raise Exception("Gagal mendownload artefak dari DagsHub.")
              

          if not any('MLmodel' in filename for root, dirs, files in os.walk('./local_model_dir') for filename in files):
              raise Exception("File MLmodel tidak ditemukan di hasil download!")
          EOF

          python build_script.py

          echo "Mencari lokasi fisik file MLmodel..."

          MODEL_DIR=$(dirname $(find ./local_model_dir -name "MLmodel" | head -n 1))
          echo "Model ditemukan di direktori: $MODEL_DIR"

          echo "Membangun Docker Image..."
          mlflow models build-docker -m "$MODEL_DIR" -n ${{ secrets.DOCKERHUB_USERNAME }}/phishing-model:latest

          echo "Mengirim Image ke Docker Hub..."
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/phishing-model:latest
